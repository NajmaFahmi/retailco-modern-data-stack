retailco:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: najma-de-learning
      dataset: retailco_dbt
      keyfile: "{{ env_var('GOOGLE_APPLICATION_CREDENTIALS') }}"
      location: US
      threads: 4